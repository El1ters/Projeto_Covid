#include "main.h"

/*Função Principal -> recebe como argumentos os parâmetros da linha de comandos e retorna o valor 0 quando o programa termina*/
int main(int argc,char *argv[]){
    CommandLine(argc, argv);
	return 0;
}
